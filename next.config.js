/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['tiossanbitcoin.vercel.app'],
    unoptimized: true,
  },
  reactStrictMode: true,
}

module.exports = nextConfig 